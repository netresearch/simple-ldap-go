# Managed by netresearch/.github/templates/
# Drift from the template is blocking CI in this repo (check-template-drift.yml).
# Record explicit exceptions under intentional-drift[] to unblock.
template: go-lib
intentional-drift:
- path: .github/workflows/ci.yml
  reason: "go-compat matrix job (Go 1.26 + 1.27 under GOTOOLCHAIN=local) \u2014 the\
    \ shared go-check workflow derives its Go version from go.mod's toolchain line\
    \ alone, so the declared 1.26.0 minimum would otherwise never be built. coverage-threshold\
    \ 77 transitional; enable-integration-tests: true (LDAP\
    \ testcontainer). E2E deliberately not enabled \u2014 this is a library (no executable\
    \ surface); the integration tier already exercises end-to-end behaviour against\
    \ a real OpenLDAP server"
- path: .github/workflows/release.yml
  reason: "cosign 3.x path bug in the reusable release workflow (create bundle file:\
    \ open : no such file or directory). 'cosign-sign: false' disables keyless signing\
    \ in this repo's release workflow; GitHub artifact attestation (attest: true default)\
    \ still provides provenance. id-token + attestations permissions added for that\
    \ attestation. Remove when the reusable release workflow upstream fixes the cosign\
    \ path issue."
